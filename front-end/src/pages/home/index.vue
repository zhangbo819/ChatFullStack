<template>
  <div>home</div>
</template>

<script setup lang="ts">
import { onActivated, onMounted, ref } from "vue";

const userList = ref([]);
const userListLoading = ref(false);

const fetchUserList = () => {
    
}


onMounted(() => {

})

onActivated(() => {

})

</script>
