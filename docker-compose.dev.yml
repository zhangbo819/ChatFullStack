version: '3.8'

services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nestdb
    ports:
      - "5432:5432"
    volumes:
      - dev_pg_data:/var/lib/postgresql/data

  adminer:
    image: adminer
    ports:
      - "8060:8080" # 将原来默认的 80 映射到 60 端口
    depends_on:
      - postgres

volumes:
  dev_pg_data: 
